// Empty type-declaration marker so `tsc --noEmit` has at least one input
// while the app code has not yet been scaffolded (T03). This file carries
// no runtime behaviour. Delete once real `.ts`/`.tsx` sources exist.
export {};
