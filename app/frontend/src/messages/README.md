# Messages — i18n placeholder

T03 marker only — no i18n runtime is wired. The first task that ships real Ukrainian copy
(T20 candidate session) picks the runtime mechanism (`next-intl`, `react-intl`, or built-in
Next i18n) and may freely reshape this directory or move the JSON files.
